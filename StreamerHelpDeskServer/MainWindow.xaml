<Window x:Class="StreamerHelpDeskServer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StreamerHelpDeskServer"
        mc:Ignorable="d"
        Title="Streamer Help Desk Server" Height="450" Width="800"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <Separator/>
                <MenuItem Header="E_xit" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="_Server">
                <MenuItem Header="_Start" Click="ServerStart_Click"/>
                <MenuItem Header="S_top" Click="ServerStop_Click"/>
                <Separator/>
                <MenuItem Header="_Configure" Click="ServerConfigure_Click"/>
            </MenuItem>
        </Menu>
        <Grid>
            <ListView ItemsSource="{Binding Messages}" Margin="5">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Time" Width="140" DisplayMemberBinding="{Binding Timestamp, StringFormat='{}{0:yyyy-MM-dd HH:mm:ss}'}" />
                        <GridViewColumn Header="Client" Width="120" DisplayMemberBinding="{Binding ClientName}" />
                        <GridViewColumn Header="Category" Width="120" DisplayMemberBinding="{Binding Category}" />
                        <GridViewColumn Header="Message" Width="350" DisplayMemberBinding="{Binding MessageText}" />
                    </GridView>
                </ListView.View>
            </ListView>
        </Grid>
    </DockPanel>
</Window>
