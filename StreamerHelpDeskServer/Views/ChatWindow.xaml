<Window x:Class="StreamerHelpDeskServer.Views.ChatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Chat with {ClientName}" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" Margin="0,0,0,10">
            <ItemsControl x:Name="MessagesItemsControl">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}" 
                                BorderThickness="0,0,0,1" Padding="5" Margin="0,2">
                            <StackPanel>
                                <TextBlock Foreground="#1E1E1E">
                                    <Run Text="{Binding SenderName, Mode=OneWay}" FontWeight="Bold"/>
                                    <Run Text=" - "/>
                                    <Run Text="{Binding Timestamp, StringFormat='{}{0:HH:mm:ss}', Mode=OneWay}" 
                                         FontSize="10" Foreground="#5A5A5A"/>
                                </TextBlock>
                                <TextBlock Text="{Binding MessageText}" TextWrapping="Wrap" Margin="0,2,0,0" Foreground="#1E1E1E"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <TextBox Grid.Row="1" x:Name="MessageTextBox" 
                 TextWrapping="Wrap" AcceptsReturn="True"
                 MinHeight="60" MaxHeight="100" Margin="0,0,0,10"
                 VerticalScrollBarVisibility="Auto"/>

        <Button Grid.Row="2" Content="Send" Width="80" HorizontalAlignment="Right"
                Click="Send_Click"/>
    </Grid>
</Window>
